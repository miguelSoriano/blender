<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:jpa="http://www.springframework.org/schema/data/jpa"
	xmlns:aop="http://www.springframework.org/schema/aop" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
		http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

	   <jpa:repositories base-package="globant.blender" />
	
		<bean class="org.springframework.orm.jpa.JpaTransactionManager" id="transactionManager">
			<property name="entityManagerFactory" ref="entityManagerFactory" />
		</bean>
	
	    <bean id="entityManagerFactory" 
	        class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
	        
	        <property name="persistenceUnitName" value="jpaData" />
	        <property name="jpaVendorAdapter">
	            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter" />
	        </property>
	        <property name="packagesToScan" >
               <list>
                  <value>globant.blender.model</value>
              </list>
            </property>
	        <property name="jpaProperties">
	            <props>
	                <prop key="hibernate.dialect">#{propertyHolder['hibernate.dialect']}</prop>
	                <prop key="hibernate.show_sql">#{propertyHolder['hibernate.show_sql']}</prop>
	                <prop key="hibernate.format_sql">#{propertyHolder['hibernate.format_sql']}</prop>
	                <prop key="hibernate.hbm2ddl.auto">#{propertyHolder['hibernate.hbm2ddl.auto']}</prop>
	                <prop key="hibernate.temp.use_jdbc_metadata_defaults">#{propertyHolder['hibernate.temp.use_jdbc_metadata_defaults']}</prop>
	                <prop key="hibernate.connection.password">#{propertyHolder['mysql.database.password']}</prop>
	                <prop key="hibernate.connection.driver_class">#{propertyHolder['mysql.database.driverClassName']}</prop>
	                <prop key="hibernate.connection.username">#{propertyHolder['mysql.database.username']}</prop>
	                <prop key="hibernate.connection.url">#{propertyHolder['mysql.database.url']}</prop>
	                
	            </props>
	        </property>
	    </bean>
	   
</beans>